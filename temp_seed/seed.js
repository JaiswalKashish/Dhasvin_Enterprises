const pg = require("pg"); const bcrypt = require("bcryptjs"); async function run() { const pool = new pg.Pool({ connectionString: process.env.DATABASE_URL }); const hash = await bcrypt.hash("admin123", 10); await pool.query("INSERT INTO users (email, name, password_hash, role) VALUES ($1,$2,$3,$4) ON CONFLICT (email) DO UPDATE SET password_hash=$3", ["admin@dhasvin.com", "Admin", hash, "admin"]); console.log("Admin seeded"); await pool.end(); } run().catch(console.error);
